# How To Contribute

## Installation

- `git clone <repository-url>`
- `cd branchlint`
- `pnpm install`

## Linting

- `pnpm lint`
- `pnpm lint:fix`
- `pnpm prettier`
- `pnpm prettier:fix`

## Running tests

- `pnpm test` – Runs the test suite on the current Ember version
